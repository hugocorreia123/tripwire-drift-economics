# Tripwire dependencies.
#
# Unlike Tolerance — whose statistics are pure standard library — this
# project trains real models over a data stream, so the numeric stack
# is genuinely required rather than optional.

numpy>=1.24
scipy>=1.11          # KS two-sample test for the drift detector
scikit-learn>=1.3    # the logistic model being retrained
pandas>=2.0          # only needed for the real-data windowing
